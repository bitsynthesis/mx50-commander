(ns mx50-commander.mx50-test
  (:require [clojure.test :refer :all]
            [mx50-commander.mx50 :as mx50]
            [mx50-commander.test-shared :as shared]))


(use-fixtures :each shared/each-fixture)


;; (deftest )
